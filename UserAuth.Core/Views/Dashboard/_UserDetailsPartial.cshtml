@using static UserAuth.Repository.helpers.Enums


@model List<UserAuth.Repository.Models.User>

<div class="container overflow-auto">
    <div class="row">
        <div class="col-md-12">
            <table class="table table-striped text-nowrap">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Email</th>
                        <th>Name</th>
                        <th>Phone</th>
                        <th>Age</th>
                        <th>Role</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model)
                    {
                        <tr>
                            <td>@user.UserId</td>
                            <td>@user.Email</td>
                            <td>@user.FirstName @user.LastName</td>
                            <td>@user.PhoneNumber</td>
                            <td>@user.Age</td>
                            <td>@((UserRole)user.Role)</td>
                            <td>
                                <a class="btn btn-primary edit-user-btn" data-user-id="@user.UserId">Edit</a>
                                @if (user.Role != (int)UserRole.Admin)
                                {
                                    <a class="btn btn-danger delete-user-btn" data-user-id="@user.UserId">Delete</a>
                                }
                                <a class="btn btn-success" asp-action="Message" asp-controller="Dashboard" asp-route-messageTo="@user.UserId">Message</a>

                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>